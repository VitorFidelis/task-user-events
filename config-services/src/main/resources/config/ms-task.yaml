server:
  port: 8082

spring:
  rabbitmq:
    host: leopard-01.lmq.cloudamqp.com
    port: 5671
    username: icfblvju
    password: aNK_WHhZSxNl336b5UofUOjs_Rp5NfJq
    virtual-host: icfblvju
    ssl:
      enabled: true

  datasource:
    url: jdbc:postgresql://ep-square-heart-ac9fnz1l-pooler.sa-east-1.aws.neon.tech/tasksdb?sslmode=require
    username: neondb_owner
    password: npg_uiYpCm38TFhk
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000
      max-lifetime: 600000
      pool-name: HikariCP
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        show_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000

logging:
  level:
    org.springframework.amqp: DEBUG
    org.springframework.rabbit: DEBUG