spring:
  application:
    name: ms-task
  config:
    import: optional:configserver:http://localhost:8888

management:
  endpoints:
    web:
      exposure:
        include: info,health

springdoc:
  swagger-ui:
    path: /swagger-ui.html  # Caminho para acessar a interface do Swagger UI
