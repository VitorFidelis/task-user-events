openapi: 3.0.3
info:
  title: Tasks Api
  description: API for managing tasks
  version: 1.0.0
servers:
  - url: http://localhost:8080
    description: Local server
tags:
  - name: Tasks
    description: Operations related to tasks
paths:
  /api/v1/tasks:
    get:
      tags:
        - Tasks
      summary: Get all tasks
      description: Retrieve a list of all tasks
      operationId: getAllTasks
      parameters:
        - $ref: '#/components/parameters/page'
        - $ref: '#/components/parameters/size'
        - $ref: '#/components/parameters/sort'
        - $ref: '#/components/parameters/status'
      responses:
        '200':
          description: A list of tasks
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedResult'
    post:
      tags:
        - Tasks
      summary: Create a new task
      description: Create a new tasks with the provided details
      operationId: createTask
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateTaskRequest'
      responses:
        '201':
          description: task created successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Task'
          headers:
            Location:
              description: URI of the created resource
              schema:
                type: string
                format: uri
                example: /tasks/123
        '400':
          description: Bad request
        '500':
          description: Internal server error

  /api/v1/tasks/{taskId}:
    get:
      tags:
        - Tasks
      summary: Get tasks by id
      description: Retrieve a task by its unique identifier
      operationId: getTaskById
      parameters:
        - name: taskId
          in: path
          required: true
          description: The ID of the task to retrieve
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: A task object
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Task'
        '400':
          description: Task not found
        '500':
          description: Internal server error
    put:
      tags:
        - Tasks
      summary: Update tasks by id
      description: Update an existing task with the provided details
      operationId: updateTaskById
      parameters:
        - name: taskId
          in: path
          required: true
          description: The ID of task to update
          schema:
            type: integer
            format: int64
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateTaskRequest'
      responses:
        '200':
          description: Task updated successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Task'
        '400':
          description: Bad request
        '404':
          description: Task not found
        '500':
          description: Internal server error

  /api/v1/tasks/{taskId}/cancel:
    patch:
      tags:
        - Tasks
      summary: Cancel a task by ID
      description: Cancel an existing task by its unique indentifier
      operationId: cancelTask
      parameters:
        - name: taskId
          in: path
          required: true
          description: The ID of the task to cancel
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: Task canceled successfully
        '404':
          description: Task Not found
        '500':
          description: Internal server error

  /api/v1/tasks/{taskId}/complete:
    patch:
      tags:
        - Tasks
      summary: Complete a task by ID
      description: Mark an existing task completed by its unique identifier
      operationId: completeTask
      parameters:
        - name: taskId
          in: path
          required: true
          description: The ID of the task to mark as completed
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: Task marked as completed successfully
        '404':
          description: Task not found
        '500':
          description: Internal server error

  /api/v1/tasks/{taskId}/execute:
    patch:
      tags:
        - Tasks
      summary: Execute a task by ID
      description: Execute an existing task by its unique identifier
      operationId: executeTask
      parameters:
        - name: taskId
          in: path
          required: true
          description: The ID of task to execute
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: Task executed successfully
        '404':
          description: Task not found
        '500':
          description: Internal server error

components:
  parameters:
    page:
      name: page
      in: query
      description: Page number
      required: false
      schema:
        type: integer
        default: 0
    size:
      name: size
      in: query
      description: Page size
      required: false
      schema:
        type: integer
        default: 10
    sort:
      name: sort
      in: query
      description: sort order(e.g.; 'title,asc')
      required: false
      schema:
        type: string
        default: 'id,asc'
    status:
      name: status
      in: query
      description: Filter by task status
      required: false
      schema:
        type: string
        enum:
          - OPEN
          - IN_PROGRESS
          - COMPLETED
          - CANCELED

  schemas:
    CreateTaskRequest:
      type: object
      properties:
        title:
          type: string
          description: The title of the task
        description:
          type: string
          description: A detailed description of the task
      required:
        - title
        - description
        
    UpdateTaskRequest:
      type: object
      properties:
        title:
          type: string
          description: the title of the task
        description:
          type: string
          description: A detailed description of the task
      required:
        - title
        - description
        
    PaginatedResult:
      type: object
      properties:
        content:
          type: array
          items:
            $ref: '#/components/schemas/Task'
        totalElements:
            type: integer
            format: int64
        totalPages:
            type: integer
            format: int64
        page:
            type: integer
            format: int64
        size:
          type: integer
          format: int64
    Task:
      type: object
      properties:
        id:
          type: integer
          format: int64
          description: The unique identifier for the task
        title:
          type: string
          description: The title of the task
        description:
          type: string
          description: A detailed description of the task
        status:
          type: string
          description: The current status of the task
          enum:
            - OPEN
            - IN_PROGRESS
            - COMPLETED
            - CANCELED
        createdAt:
          type: string
          format: date-time
          description: The date and time when the task was created
        updatedAt:
          type: string
          format: date-time
          description: The date and time when the task was last updated
        deletedAt:
          type: string
          format: date-time
          description: The date and time when the task was deleted